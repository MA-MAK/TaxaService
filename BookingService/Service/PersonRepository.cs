using System;
using MongoDB.Driver;
using MongoDB.Bson;
/* 
public interface IPersonRepository
{
    Task<Person?> GetPerson(Guid personId);
    // Task<IEnumerable<Person>?> GetProductItemListByCategory(ProductCategory category); Task<Guid> AddProductItem(ProductItemDTO item);
    // Task<long> AddImageToProductItem(Guid productId, Uri imageURI);
}

public class PersonMongoDBService : IPersonRepository
{
    public PersonMongoDBService(ILogger<PersonMongoDBService> logger, MongoDBContext dbcontext)
    {
        _ilogger = logger;
        _database = dbcontext.Database;
    }
    public async Task<Person?> GetPerson(Guid personId)
    { 

    }
} 
*/